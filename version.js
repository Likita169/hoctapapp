// Sửa DUY NHẤT dòng này mỗi khi deploy bản mới (1.0.0 -> 1.0.1 -> 1.1.0 ...).
// Cả các file trong thư mục js/ (hiện số phiên bản trong tab Thống kê) và
// service-worker.js đều tự lấy theo số này, không cần sửa gì thêm.
const APP_VERSION = '1.13.2';